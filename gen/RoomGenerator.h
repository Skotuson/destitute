#ifndef ROOMGENERATOR_H
#define ROOMGENERATOR_H

#include <cstdlib>

const size_t MAX_ROOM_SIZE = 15,
             MIN_ROOM_SIZE = 5;

class RoomGenerator {
    public:
        static void Generate ( void );
    private:
};

#endif